# Release Artifacts

This folder contains a compact, Git-friendly checkpoint bundle intended for
quick verification and reproducibility.

Current bundle:

- `final_patient_horizon/model/best_model.pt`
- `final_patient_horizon/eval/metrics.json`

Larger local datasets and full experiment trees stay outside git under:

- `data/sepsis_causal_artifacts/`
